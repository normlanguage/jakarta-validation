package jakarta.validation

import jakarta.validation.constraints.NotBlank
import jakarta.validation.constraints.Size

class Registration {
  @NotBlank()
  @Size(min: 3, max: 32)
  String username
}

Void main() {
  var registration = Registration(username: "Norm")
  printLine(registration.username)
}
